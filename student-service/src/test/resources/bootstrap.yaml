spring:
  application:
    name: student-service
  cloud:
    config:
      enabled: false
      fail-fast: false
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false